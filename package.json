{
  "name": "ironwild",
  "version": "0.1.0",
  "description": "IRONWILD - an original open-world machine-hunting prototype (browser, Three.js)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npm run build && playwright test",
    "test:e2e:chunked": "node scripts/run-e2e-chunked.mjs",
    "assets:validate": "node scripts/validate-assets.mjs",
    "assets:create-cert": "node scripts/create-cert-asset.mjs",
    "verify": "npm run lint && npm test && npm run test:e2e"
  },
  "license": "MIT",
  "dependencies": {
    "three": "^0.166.0",
    "vite": "^5.4.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "eslint": "^10.8.1",
    "globals": "^17.11.0",
    "gltf-validator": "^2.0.0-dev.3.10",
    "vitest": "^4.1.11"
  }
}
