# IRONWILD — V3 Addendum (extends ARCHITECTURE.md + ARCHITECTURE_V2.md)

Third upgrade wave. All v1/v2 contracts still hold. RULE ZERO: no regressions.

## New shared state (already in core/state.js)

- `G.xp` `{ level, cur, next }` — owned by systems/xp.js.
- `G.bossNear` bool — written by machines/ai.js when the Monarch is within 80u of the player.

## New events (core/events.js header)

`xpGain {amount, reason}` · `levelUp {level}` · `meleeSwing {hit}`

## Rows (each row = one agent; files are disjoint)

| Row | Files | Scope |
|---|---|---|
| world-rich | `src/world/environment.js`, `src/world/props.js`, `src/world/terrain.js` | **Clouds**: ~14 drifting flat-shaded cloud blobs (merged spheres, MeshLambert white, transparent 0.85, castShadow OFF) at y≈120-160 circling slowly with the wind. **Water upgrade**: gentle vertex waves on the water plane (cheap sin displacement in onBeforeCompile or a low-seg plane animated on CPU ≤ 32×32) + slightly varied opacity near shore. **Underwater**: when camera.y < waterLevel → dense blue fog (near 1/far 24, color #1d4a56) and muffled ambience cue via bus 'notify'? NO — just fog + a fullscreen blue-tint div owned here. **Flora richness**: instanced flowers (~300, two variants: white/yellow heads), mushrooms (~80, red caps), fallen logs (~25, cylinder + moss cap). Quality scaling: flower/mushroom counts ×0.5 when G.settings.quality==='low'. Keep ALL v1/v2 exports intact (biomeAt, isConcealed, spawnPickup, foam, weather reactions). |
| machines-content | `src/machines/machines.js` | Two new bodies following existing builder patterns: `mirefang` (lake crocodile — low flat body, jaw, tail paddle; hp 110; weak points: eye ×2.2 hp35, belly seam ×1.6 hp50; loot shards×5 oil×2), `monarch` (colossal saurian world-boss — towering frame ~9u tall, crown of antennae, glowing chest furnace weak point 'furnace' ×1.5 hp220 plus two knee pistons ×1.8 hp70; hp 1400; slow relentless walk along a huge border loop; loot shards×40 oil×12 medicine×3; name label 'the Monarch'). Register both in createMachine factory + LOOT table. Do NOT touch ai.js. |
| machines-ai | `src/machines/ai.js` | Behaviors for the new bodies + world systems: **Mirefang** — dormant submerged at lake bottom spots (only nostril glow above water); ambushes swimmers/player entering water within 14u (burst lunge 20 dmg, drag brief); on land sluggish short chase; returns to water if player flees far. **Monarch** — never sprints; stomp AOE (r4, 35 dmg, telegraphed by foot raise 0.8s) when player < 6u; sweeping tail swipe (arc behind, 25 dmg) when player behind > 2s; every 25% hp lost → enrage: speed +30%, antenna roar emits noise r60; dies spectacularly (multi-explosion sequence 2s). Writes `G.bossNear` each frame (monarch within 80u). **Respawn**: dead non-alpha machines respawn at their anchor after 90s (fresh hp/state; alphas respawn too but only after 240s; Monarch/Vantage never respawn). **Herd flee**: when one bramblehorn flees, others within 25u flee same direction. Keep ALL v1/v2 behavior intact. |
| melee-spear | `src/player/spear.js` (NEW), `src/player/player.js` | **Spear quick-melee**: KeyF swings the hunting spear (new primitive spear mesh attached to right hand anchor 'handR' — ADD that named anchor in player.js if absent): 2.4u arc hit in camera-forward cone ±55°, 26 dmg body / 39 on weak-point contact (machines' standard hit() path via closest weakPoint/bodySphere within range — implement a small sphere query against G.machines), cooldown 0.8s, small forward lunge 1.2u, emits 'meleeSwing' {hit} + 'machineHit'-compatible feedback through machine.hit + bus events so FX/audio react identically to arrows. Export createSpear(), updateSpear(dt). player.js: add 'handR' anchor + swing anim channel (right arm sweep driven by spear module via G.player.meleeT 0..1 it writes). Cannot swing while swimming/drawing bow. |
| progression | `src/systems/xp.js` (NEW), `src/ui/hud.js` | **XP**: exports createXp(), updateXp(dt), grantXp(amount, reason). Sources wired via bus: machineDied (skitter 20, bramblehorn 30, rendclaw 45, ironmaw 80, duskwing 60, bulwark 70, mirefang 55, monarch 500; alpha ×1.5), pickup +5, machineScanned +15. Level curve: next = 100 × level^1.35 rounded; on level-up: +1 skillPoint, full heal? NO — +1 skill point + toast + 'levelUp'. Persisted fields added into save.js? save.js is NOT yours — store xp inside G.xp only; integrator will note save.js amendment separately (save.js owner: add xp to snapshot in a follow-up micro-task by systems owner IF trivially safe — otherwise skip). HUD: slim XP bar pinned above the health bar (violet #b48cff fill, level badge 'LV n'), pulse on gain. Keep ALL v1/v2 HUD elements intact. |
| tips | `src/ui/tips.js` (NEW) | Contextual one-time hints, self-contained DOM stack bottom-right above minimap area: first machine within 40u → '[Q] Focus scan reveals weak points'; first time hp<50% → '[H] Use medicine'; first time crouched in grass (G.player.concealed) → 'Concealed — machines see you barely'; first fireArrows acquired → '[X] Switch arrow type'; first storm → 'Storms pass. Machines hunt by sound.'; first contract complete → 'New contracts appear over time'. Each shows once per browser (localStorage 'ironwild-tips'), 5s fade, max 1 visible. Exports createTips(), updateTips(dt). |
| balance-audit | `docs/BALANCE.md` (NEW — only file you may write) | READ-ONLY code audit: extract all combat/economy constants (machine hp/dmg/cd, weapon dmg, XP table, craft costs, loot yields, stamina costs) from src/**, compute TTK tables (player vs each machine at skill ranks 0/1, arrows vs spear vs fire), economy loops (arrows/day, medicine sustainability), and flag outliers (e.g., 'rendclaw kills unarmored player in 3.4s standing still — intended predator or too fast?', 'ironmaw loot 10 shards makes contracts redundant'). Write docs/BALANCE.md with tables + concrete recommended constant diffs (file/line/old→new). DO NOT edit any source file. |
| audio-v3 | `src/audio/audio.js` | Boss layer: when G.bossNear true → deep war-drum pulse + low choir-ish detuned saw pad crossfade in (setTargetAtTime); victory sting on 'killStreak' count>=3 (short brass-y triad burst); melee whoosh on 'meleeSwing' (filtered noise sweep, hit variant adds thud); levelUp fanfare on 'levelUp' (rising major arp, brighter than skillUp). Keep everything else intact. |
| integrator-v3 | `src/main.js` | LAST. Wire: createSpear()/updateSpear(dt) after bow step; createXp()/updateXp(dt) after quests; createTips()/updateTips(dt) after minimap; renderer.toneMapping = THREE.ACESFilmicToneMapping, toneMappingExposure 1.05 at boot. Adapt to ACTUAL exports; feature-check defensively; build must pass. |

## Verification bar (integrator + orchestrator)

- Build green; boot clean; spear hits a staged machine (weak + body); XP grants on kill and levels up
  after enough kills; Monarch spawns far away and patrols; Mirefang ambushes a swimmer; respawn works
  (kill skitter, wait/force 90s, alive again at anchor); tips appear once; BALANCE.md exists with tables.
